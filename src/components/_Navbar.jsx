
  
  const Navbar = () => {
    /* const auth = getAuth(app);
    const navigate = useNavigate();
    
    const logOut = async () => {
        await signOut(auth);
        navigate('/login');
      }; */

    return (
       <>
       </>
    );
  };
  
  export default Navbar;